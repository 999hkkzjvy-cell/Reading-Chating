# 以读攻独 · 部署优化方案

> 制定日期：2026-07-10
> 当前架构：GitHub Pages + Supabase（免费 tier）
> 目标：优化国内访问速度、扩展存储容量、控制成本

---

## 1. 现状与问题

### 当前架构
```
GitHub Pages（美国节点） → 静态前端
Supabase 免费 tier       → 数据库 + Storage + Auth
```

### 三个核心问题

| 问题 | 现状 | 瓶颈 |
|------|------|------|
| 存储容量 | Supabase Storage 免费 1GB | PDF 文件增长快，即将不够用 |
| 访问速度 | GitHub Pages 美国节点 | 国内用户 3-8 秒首屏加载，偶尔不稳定 |
| 用户扩展 | 当前 60 用户，未来可能数百 | Supabase API 免费 5 万次/月可能不够 |

---

## 2. 目标架构

```
┌────────────────────────────┐
│  Cloudflare Pages（全球 CDN）│  ← 前端静态文件，免费无限带宽
│  自定义 .com 域名           │     国内边缘节点加速 1-3 秒
└──────────────┬─────────────┘
               │
    ┌──────────┴──────────┐
    │                     │
┌───▼──────────┐  ┌───────▼──────────────┐
│  Supabase    │  │  Cloudflare R2       │
│  数据库 + Auth│  │  PDF + 大文件存储     │
│  API + 头像  │  │  10GB 免费            │
│  免费~$25/月 │  │  零带宽费             │
└──────────────┘  └──────────────────────┘
```

### 服务分层

| 层级 | 服务 | 费用 | 覆盖内容 |
|------|------|------|---------|
| 前端部署 | Cloudflare Pages | 免费 | HTML/CSS/JS，无限带宽 |
| 域名 | Cloudflare Registrar | ~$10/年 | .com 域名 |
| 数据库 + Auth | Supabase | 免费~$25/月 | 文本数据 + 用户认证 |
| 小型图片 | Supebase Storage | 免费 1GB | 封面图、头像、聊天图片 |
| PDF + 大文件 | Cloudflare R2 | 免费 10GB | 共读 PDF、大附件 |
| 缓存 + 安全 | Cloudflare CDN | 免费 | DDoS 防护、全球加速 |

### 年度总成本：~¥75（仅域名费用）

---

## 3. 实施计划

### P0：GitHub Pages → Cloudflare Pages（预计 5 分钟）

**目的**：解决国内访问速度问题

1. 注册 [Cloudflare](https://cloudflare.com) 账号
2. Workers & Pages → Pages → 授权 GitHub 仓库
3. 选择 `Reading-Chating` 仓库 → `main` 分支
4. 构建设置：无需构建命令，输出目录 `/`
5. 部署完成后获得 `xxx.pages.dev` 域名

**零代码改动，Git push 自动部署。**

### P1：PDF/大文件迁移到 Cloudflare R2（预计 1-2 小时）

**目的**：解决 Supabase Storage 容量瓶颈

1. 在 Cloudflare 控制台开通 R2
2. 创建 bucket（如 `reading-chating-files`）
3. 获取 Access Key ID + Secret Access Key
4. 修改 `src/uploads.js`：PDF 和聊天图片上传从 Supabase Storage 切到 R2
5. 修改前端显示逻辑：R2 文件 URL 用自定义域名或 worker 代理
6. 可选：Supabase Storage 只保留封面图和头像

### P2：自定义域名（预计 10 分钟）

**目的**：专业品牌 + 域名统一管理

1. Cloudflare Registrar 购买 .com 域名（约 $10/年，~¥75）
2. DNS 自动配置，无需手动操作
3. Cloudflare Pages → 自定义域 → 绑定购买的域名
4. R2 bucket → 绑定同名子域名（如 `files.your-domain.com`）

**不需要 ICP 备案。** .com 域名 + 境外服务器 = 国内正常访问。

### P3：Supabase Pro 升级（按需）

**触发条件**：API 请求超过 5 万次/月，或数据库超过 500MB

1. Supabase Dashboard → 升级到 Pro（$25/月）
2. API 请求从 5 万 → 1000 万次/月
3. 数据库从 500MB → 8GB
4. Storage 从 1GB → 100GB

**当前用户量下不需要。** 500 用户以下免费 tier 足够。

### P4：PWA 化（预计 1 小时）

**目的**：手机端接近原生 App 体验

1. 添加 `manifest.json`（图标、名称、主题色）
2. 添加 `service-worker.js`（离线缓存）
3. HTML 中注册 service worker
4. 用户可在手机桌面安装，全屏打开

---

## 4. 域名购买指南

### 平台对比

| 平台 | .com 年费 | 国内访问 | 实名要求 | 推荐 |
|------|----------|---------|---------|------|
| Cloudflare Registrar | ~$10 | ✅ | 不需要 | ⭐⭐⭐⭐⭐ |
| Namecheap | ~$11 | ✅ | 不需要 | ⭐⭐⭐⭐ |
| 阿里云/腾讯云 | ~¥65 | ✅ | 需要实名 | ⭐⭐⭐ |

### 后缀选择

| 后缀 | 国内访问 | 备案要求 | 推荐 |
|------|---------|---------|------|
| **.com** | ✅ 正常 | 不需要 | ⭐⭐⭐⭐⭐ |
| **.org** | ✅ 正常 | 不需要 | ⭐⭐⭐⭐ |
| .cn | ✅ | **必须备案** | ❌ 个人论坛做不了 |

### 为什么不选 .cn

.cn 域名必须提交 ICP 备案，个人主体备案不允许做论坛/社区类网站，基本不可能通过。`.com` 域名在境外注册商购买 + 境外服务器，完全不需要备案，国内正常访问。

---

## 5. 免费额度对比

| 服务 | 免费额度 | 500 用户是否够 | 瓶颈 |
|------|---------|--------------|------|
| Cloudflare Pages | 无限带宽 | ✅ 完全够 | 无 |
| Cloudflare R2 | 10GB + 1000万次读取/月 | ✅ 够 | PDF 超过 10GB 才需要付费 |
| Supabase 数据库 | 500MB | ✅ 够 | 纯文本增长极慢 |
| Supabase Auth | 50,000 MAU | ✅ 完全够 | — |
| Supebase API | 50,000 次/月 | ⚠️ 可能不够 | 每个页面刷新都调 API |
| Supabase Storage | 1GB | ❌ 不够 | PDF 是主要问题 |

### Supebase Pro（$25/月）解锁

| 资源 | 免费 | Pro |
|------|------|-----|
| API 请求 | 5 万/月 | 1000 万/月 |
| 数据库 | 500MB | 8GB |
| Storage | 1GB | 100GB |
| MAU | 5 万 | 10 万 |

---

## 6. App/小程序可行性

| 类型 | 个人主体 | 成本 | 可行性 |
|------|---------|------|--------|
| iOS App | 需开发者账号（$99/年）+ 软著 | 高 | ❌ 论坛类审核难 |
| Android App | 需软著 + 应用市场上架审核 | 中 | ❌ 论坛类审核难 |
| 微信小程序 | 个人主体只允许工具类 | 中 | ❌ 论坛类需要企业资质 |
| **PWA** | 不需要任何审核 | 免费 | ✅ 最佳替代方案 |

**结论：App 和小程序对个人论坛基本不可行。PWA 可以达到接近原生 App 的体验，无需审核，建议采用。**

---

## 7. 实施优先级

| 优先级 | 任务 | 耗时 | 解决什么问题 |
|--------|------|------|-------------|
| 🔴 P0 | 迁移到 Cloudflare Pages | 5 分钟 | 国内访问速度 |
| 🟡 P1 | R2 分流 PDF 存储 | 1-2 小时 | Storage 容量 |
| 🟡 P2 | 购买 .com 域名 | 10 分钟 | 品牌 + 专业度 |
| 🟢 P3 | Supabase Pro（按需） | 5 分钟 | API 请求不够时 |
| 🟢 P4 | PWA 化 | 1 小时 | 手机端体验 |